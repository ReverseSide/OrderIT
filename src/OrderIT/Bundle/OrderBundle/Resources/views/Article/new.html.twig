{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Article creation</h1>

    <div id="test">{{ form(form) }}</div>

        <ul class="record_actions">
    <li>
        <a href="{{ path('article') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
