{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Commenter la demande de modification</h1>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.comment) }}
    {{ form_row(form.commenter) }}

    <div class="hide">
        {{ form_end(form) }}
    </div>



{% endblock %}
