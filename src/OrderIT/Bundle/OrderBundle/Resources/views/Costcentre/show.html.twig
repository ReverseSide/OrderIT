{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Costcentre</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idcostcentre</th>
                <td>{{ entity.idCostcentre }}</td>
            </tr>
            <tr>
                <th>Costcentre</th>
                <td>{{ entity.costcentre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('costcentre') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('costcentre_edit', { 'id': entity.idCostcentre }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
